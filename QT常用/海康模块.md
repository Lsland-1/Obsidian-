```cpp         
圆查找:
IMVSCircleFindModuTool * pCirFindObject = (IMVSCircleFindModuTool *)(*m_pVmSol)["circle.圆查找1"];
if (NULL == pCirFindObject) return;
CircleFindParams * pCirFindParam = pCirFindObject->GetParamObj();
if (NULL == pCirFindParam) return;
void * pParamData = pCirFindParam->GetControlInfo();
if (NULL == pParamData) return;
qlonglong pRenderData = (qlonglong)pParamData;
ui->axWidgetParam->dynamicCall("SetParamsInfo(qlonglong)", pRenderData);
```
